#include "localidad.h"

void loc_mostrar1(eLocalidad aux)
{
    printf("\nId: %d Nombre: %s", aux.id, aux.nombreLocalidad);
}

void loc_listarLoc(eLocalidad xd)
{
    for(i=0;i<lenght;i++)
{
    listadoLocalidades[i].id=i*3;
    listadoLocalidades[i].estado=1;
    strcpy(listadoLocalidades[i].nombre,"Maria");
}
//ME re perdi xd
}
